

[pytest]
addopts = --strict-markers
markers =
    regression: runs all regression tests
    sanity : runs all sanity tests
    dev : Automation developers can use this while debugging tests
    environment : Environment preparation for new brands